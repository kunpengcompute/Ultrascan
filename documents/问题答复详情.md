# 3.31 问题答复

我把这 12 个问题都对着当前代码重新过了一遍，下面按问题逐条回答。先给一个总判断，方便我们对齐：

1. 当前 PBE **功能链路是通的**，UT 也说明语义基本正确。
2. 但当前实现里有不少“**为了先复用现有 FDR/Neo 框架而做的借壳设计**”，这会让代码看起来像 Neo 复制过来的。
3. 现在真正的性能瓶颈，已经不只是 `key` 提取，而是 **结构过重 + confirm 太慢 + 极短规则集不适配**。
4. 你提到的几个疑点里，最重要的两个是：
   - `choosePbeEngine` 目前确实主要是复用 Neo 的 proto/bucket 选择逻辑
   - “规则数上限改成 100000000” 现在**不能只改一行 check**，结构上还不成立

---

## 1. `fdrBuildProtoInternal` 里的 `choosePbeEngine` 和 `chooseNeoFdrEngine` 有什么区别？是不是复制 Neo 的逻辑？这一部分在做什么？

对应代码：
- [fdr_compile.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_compile.cpp:878)
- [fdr_engine_description.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_engine_description.cpp:214)
- [fdr_engine_description.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_engine_description.cpp:319)

**结论**
是的，当前 `choosePbeEngine(...)` 和 `chooseNeoFdrEngine(...)` 的逻辑几乎是同构的，差别主要只有：
1. Neo 用 `getNeoFdrDescriptions(...)`，engine id 是 `1`
2. PBE 用 `getPbeDescriptions(...)`，engine id 是 `2`

定义上它们甚至都只塞了一个相同形状的描述：
- Neo: `{1, 64, 8, 0}`
- PBE: `{2, 64, 8, 0}`

对应代码：
- [fdr_engine_description.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_engine_description.cpp:62)
- [fdr_engine_description.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_engine_description.cpp:68)

**这一部分到底在做什么**
它**不是**在构建 PBE 自己的 22-bit 一级哈希表。  
它做的是：

1. 选一个 `FDREngineDescription`
2. 用这个描述去跑：
   - `assignStringsToBuckets(...)`
   - `addIncludedInfo(...)`
   - `HWLMProto(...)`
3. 让 PBE 先复用现有 FDR/Neo 的 proto 构建链路和外层编译框架

也就是说，`choosePbeEngine(...)` 选出来的 `bits/stride/numBuckets`，主要服务于：
- bucket 分配
- included info
- 外围 `FDRCompiler` / proto 流水

而**真正的 PBE L1/L2 哈希结构**，是在后面：
- [buildPBEArtifacts(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:727)
里单独构建的。

**这是不是有问题**
不是“功能错误”，但确实是一个**设计借壳**。

当前这样做的好处：
1. 快速接入现有 FDR 编译链路
2. 可以直接复用 bucket/included/外围 table builder
3. 少改很多现有代码

当前这样做的问题：
1. `choosePbeEngine` 的 heuristics 其实还是 Neo 风格的
2. 这些 heuristics 不一定最适合 PBE
3. 代码读起来会让人误以为“PBE 的编译策略 = Neo 的编译策略”

**更准确地说**
当前 PBE 是：
- **外层 proto/bucket 选择借用 Neo/FDR**
- **内层真正扫描结构用 PBE 自己的 artifacts/blob**

所以它不是“完全复制 Neo”，但确实有一层明显的 Neo 兼容壳。

---

## 2. 进入 PBE 的前置条件需要调整，规则数上限暂定为 `100000000`

对应代码：
- [analyzePBEFeasibility(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:771)
- [canBuildPBE(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:857)

当前上限判断：
- [pbe_compile.cpp:808](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:808)

```cpp
if (lits.size() > std::numeric_limits<u16>::max()) {
    local.reason = PBEFeasibilityReason::TOO_MANY_LITERALS;
    ...
}
```

**这里不能直接改成 `100000000`**
因为当前结构本身还不支持这么大的规则规模，至少有 4 个硬限制。

**限制 1：`ruleIndex` 现在是 `u16`**
对应结构：
- [pbe_compile.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:51)
- [pbe_runtime.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_runtime.h:58)

字段：
- `u16 ruleIndex[PBE_RULE_SLOTS_PER_ENTRY]`

写入点：
- [pbe_compile.cpp:612](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:612)

所以只要 literal 下标超过 `65535`，这里就已经不成立了。

**限制 2：L1 的 count 字段只有 14 bit**
对应编码：
- [pbe_compile.cpp:108](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:108)
- [pbe_compile.h:17](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:17)
- [pbe_compile.h:19](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:19)

L1 value 的布局是：
- 低 18 bit：secondary offset
- 高 14 bit：连续 L2 entry 数

所以单个一级 key 最多只能表示：
- `16383` 个 L2 entry
- 每个 L2 entry 4 条规则
- 也就是单 key 最多 `65532` 条规则

如果很多规则落到同一个 key，上限会先炸掉。

**限制 3：`literalBlob` 和 `litOffset` 还是 `u32`**
对应结构：
- [pbe_compile.h:63](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:63)
- [pbe_runtime.h:70](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_runtime.h:70)

字段：
- `u32 litOffset`
- `u32 literalBlobSize`

如果总规则字节量超过 4GB，这里也不成立。

**限制 4：实际内存成本会非常大**
即使不碰类型溢出，`100000000` 条规则在当前结构下：
- `ruleMeta`
- `literalBlob`
- L2 entries
- bucket map
都会是非常重的内存压力。

**所以正确结论是**
“把规则数上限改成 `100000000`” 不是一个 check 改动，而是一个**结构级改造**。

至少需要：
1. `ruleIndex` 从 `u16` 改成 `u32`
2. 重新设计 L1 value 里的 `count|offset` 编码
3. 重新评估 `literalBlobSize/litOffset`
4. 重新评估构建期内存模型

**我建议**
这一点如果要做，我建议单独开一轮改造计划，而不是直接把：
- [pbe_compile.cpp:808](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:808)
改成 `100000000`

否则代码会变成“前置检查放行了，但后面结构仍然装不下”。

---

## 3. `buildExtractDescriptor()` 里 `bextToKeyBit` 保存的是什么？“packed bit 到 key bit 的重映射”是什么意思？有没有排序？

对应代码：
- [buildExtractDescriptor(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:125)

关键逻辑：
- [pbe_compile.cpp:140](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:140)
- [pbe_compile.cpp:148](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:148)
- [pbe_compile.cpp:150](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:150)
- [pbe_compile.cpp:151](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:151)

**先说结论**
`bextToKeyBit` 保存的**不是 source bit index 本身**，而是：

**“`bext` 压缩结果里第 `i` 个 packed bit，应该回填到 key 的哪一位”**

也就是：
- packed bit index -> key bit index

**为什么需要它**
因为当前 key 的位编号，是按 `selectors` 的顺序定义的。  
而 `bext`/packed extract 的输出顺序，不是 selector 顺序，而是：

**按 source bit 在 `window64` 里的位置从小到大压缩**

这两个顺序通常不一样，所以必须做一次重映射。

**当前实现过程**
`buildExtractDescriptor()` 先构造：

```cpp
(bitIndex, selectorIndex)
```

其中：
- `bitIndex` = 这个 selector 对应的 `window64` 源 bit 位置
- `selectorIndex` = 这个 selector 在 key 里的逻辑位序

然后：
- [pbe_compile.cpp:148](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:148)
对 `bitOrder` 做排序

```cpp
std::sort(bitOrder.begin(), bitOrder.end());
```

默认按 pair 的第一元素排序，也就是：

**按 source bitIndex 升序排序**

然后：
- [pbe_compile.cpp:150](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:150)
把这些 source bits 填进 `bextMask`
- [pbe_compile.cpp:151](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:151)
把对应的原始 `selectorIndex` 填进 `bextToKeyBit[i]`

**所以它有没有排序**
有，而且是：

**按 source bit index 从小到大排序**

不是按 key bit 从大到小，也不是按 key bit 从小到大。

**举个例子**
假设 selector 顺序是：

1. `selector0 -> source bit 11`
2. `selector1 -> source bit 2`
3. `selector2 -> source bit 20`

那么：
- key bit 0 对应 source bit 11
- key bit 1 对应 source bit 2
- key bit 2 对应 source bit 20

排序后 source bits 变成：
- 2
- 11
- 20

所以：
- packed bit 0 来自 source bit 2，它应该写回 key bit 1
- packed bit 1 来自 source bit 11，它应该写回 key bit 0
- packed bit 2 来自 source bit 20，它应该写回 key bit 2

于是：

```text
bextToKeyBit = [1, 0, 2]
```

**这就是“packed bit 到 key bit 的重映射”**

**这对运行期的意义**
运行期在：
- [pbeExtractKeyBext(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:262)
- [pbeRemapPackedBits(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:240)

会先得到 packed bits，再按 `bextToKeyBit` 回填回逻辑 key。

所以这个设计是为了：
1. 让 `bextMask` 可以按 source bit 升序直接工作
2. 又不破坏当前 key 的 selector 位定义

这一步是合理的。

---

## 4. `buildPrimaryBitmap()` 是怎么实现的？结构是什么样的？举个例子

对应代码：
- [buildPrimaryBitmap(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:165)
- [pbePrimaryBitmapSet(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:157)

对应结构：
- [PBEPrimaryHashTable](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:43)
- [PBEPrimaryHashBitmap](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:47)

**结构本质**
它是一个**固定大小的 dense bitmap**，不是只给非空项单独建条目。

也就是说：
1. 一级哈希表 `offsets` 的项数固定是 `2^22`
2. 位图的 bit 数也固定对齐到这个项数
3. 某个一级表项非空，就把对应 bit 置 1
4. 否则保持 0

大小计算：
- [pbePrimaryBitmapBytes(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:120)

```cpp
(primaryCount + 7) / 8
```

所以当 `primaryCount = 2^22` 时：
- bitmap bytes = `524288`

这和你 `PBEInspect` 输出里的：
- `bitmapBytes=524288`
是一致的。

**构建流程**
1. 清空 `bits`
2. 按 `primaryCount` 分配固定字节数并全部置 0
3. 遍历 `primaryHashTable.offsets`
4. 如果 `offsets[i] != 0`，就对 `bits[i/8]` 的 `(i%8)` 位设 1

关键判断：
- [pbe_compile.cpp:173](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:173)

```cpp
if (primaryHashTable.offsets[i]) {
    pbePrimaryBitmapSet(&primaryHashBitmap->bits, i);
}
```

**举个小例子**
假设一级表只有 16 项：

```text
offsets[0] = 0x40001
offsets[1] = 0
offsets[2] = 0
offsets[3] = 0x40008
offsets[4] = 0
offsets[5] = 0
offsets[6] = 0
offsets[7] = 0
offsets[8] = 0
offsets[9] = 0x40020
offsets[10..15] = 0
```

那 bitmap 需要 2 个字节。

第 0 个字节表示索引 0..7：
- index 0 有值 -> bit0 = 1
- index 3 有值 -> bit3 = 1

所以：
```text
bits[0] = 00001001b = 0x09
```

第 1 个字节表示索引 8..15：
- index 9 有值 -> bit1 = 1

所以：
```text
bits[1] = 00000010b = 0x02
```

**运行期判断方式**
- [pbePrimaryBitmapHasValue(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:410)

```cpp
bitmap[idx / 8] & (1U << (idx % 8U))
```

**所以回答你的问题**
它不是“只存非空项”的稀疏结构，  
而是：

**项数固定为 L1 最大项数，只有部分 bit 有值的 occupancy bitmap。**

---

## 5. `fdrBuildTableInternal()` 这里会重建一遍 `artifacts`，原先的 `artifacts` 是否正常释放了？

对应代码：
- [fdrBuildTableInternal(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_compile.cpp:967)

这里确实做了两份 artifacts：

1. proto 阶段：
- [fdr_compile.cpp:905](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_compile.cpp:905)
把 `pbeArtifacts` 存进：
```cpp
proto->pbeArtifacts = ue2::make_unique<PBECompileArtifacts>(...)
```

2. table 阶段：
- [fdr_compile.cpp:976](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_compile.cpp:976)
又局部构造：
```cpp
PBECompileArtifacts rebuiltArtifacts;
```

**为什么重建**
因为 proto 构建后，`proto.lits` 还可能被后续流程改写，尤其是：
- literal id 被 Rose 程序 offset 重写

所以 table build 必须基于最终 `proto.lits` 重建，否则会拿到 stale id。

这一步是必要的。

**原来的 artifacts 会不会泄漏**
不会，当前是 RAII，生命周期是正常的。

具体来说：

1. `proto->pbeArtifacts`
- 由 `unique_ptr` 持有
- 会在 `HWLMProto` 析构时自动释放

2. `rebuiltArtifacts`
- 是栈上局部变量
- `fdrBuildTableInternal(...)` 返回时自动销毁

3. `pbeBlob`
- 是 `bytecode_ptr<u8>`
- 成功时 move 进 `FDRCompiler`
- 失败时离开作用域自动释放

**所以没有内存泄漏**
但确实有一个现象：

**会暂时存在两份 artifacts**
- 一份缓存的 proto artifacts
- 一份 table build 的 rebuiltArtifacts

这不是 leak，而是**冗余临时占用**

**我的判断**
当前设计是“正确但不够干净”：
1. 正确性上需要重建
2. 生命周期上没问题
3. 但 proto 阶段缓存的那份 artifacts 在 table 阶段已经不再参与最终 blob 生成，更多是调试/中间产物

---

## 6. 一级 / 二级哈希表的构建流程，需要更详细描述

对应代码：
- [buildHashTables(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:532)

我按真正流程展开。

### 第一步：确定 L1 固定大小
- [pbe_compile.cpp:544](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:544)

```cpp
const u32 tableSize = 1U << PBE_KEY_BITS;
primaryHashTable->offsets.assign(tableSize, 0);
```

当前 `PBE_KEY_BITS = 22`：
- [pbe_compile.h:16](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:16)

所以 L1 永远是：
- `2^22 = 4,194,304` 项

每项一个 `u32`。

### 第二步：预留 L2[0] 为空项
- [pbe_compile.cpp:547](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:547)

```cpp
secondaryHashTable->push_back(PBESecondaryHashEntry{});
```

这样 `secondaryOffset = 0` 可以自然表示空引用。

### 第三步：计算每条规则的 `keyValue` 和 `keyMask`
- [pbe_compile.cpp:556](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:556)
- [computeKeyValueMaskForLiteral(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:45)

对每条 literal：
1. 遍历 selectors
2. 看这个 selector 对应 bit 在该规则里是不是 care
3. care 就在 `keyMask` 相应位置 1
4. care 且值为 1，就在 `keyValue` 相应位置 1

所以：
- `keyValue` = 规则在 cared selectors 上的值
- `keyMask` = 哪些 selectors 对这条规则是 cared

### 第四步：决定规则进哪个 L1 bucket
关键逻辑：
- [pbe_compile.cpp:562](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:562)

```cpp
const u32 bucketKey = (keyMask == fullMask) ? keyValue : 0U;
```

也就是当前的**单 key 策略**：

1. 如果这条规则对 22 个 key bit 全部 care
- 进 exact bucket
- `bucketKey = keyValue`

2. 如果存在 don't-care bit
- 进 wildcard bucket
- `bucketKey = 0`

所以当前所有 partial mask 规则都会堆到：
- `L1[0]`

### 第五步：按 bucketKey 聚合规则
- [pbe_compile.cpp:550](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:550)

```cpp
std::map<u32, std::vector<u32>> keyToLiteralIndexes;
```

最后得到：
- 一个 key
- 对应一组 literal index

### 第六步：给每个一级 key 分配连续 L2 entries
对每个 key bucket：
- [pbe_compile.cpp:576](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:576)

```cpp
entryCount = ceil(bucketRules / 4)
```

因为每个 L2 entry 固定 4 条规则：
- [pbe_compile.h:20](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:20)

然后：
- [pbe_compile.cpp:594](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:594)
记录这个 key 对应的起始 `secondaryOffset`

L1 value 的编码：
- [encodePrimaryValue(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:108)

格式是：

```text
u32 primaryValue =
    [ high bits: entryCount ][ low 18 bits: secondaryOffset ]
```

### 第七步：填充每个 L2 entry
每个 chunk 最多 4 条规则，填一个 `PBESecondaryHashEntry`

对应结构：
- [pbe_compile.h:51](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:51)

填充内容：

1. `ruleIndex[slot]`
- 指向 `ruleMeta` 的下标

2. `keyValue[slot]` / `keyMask[slot]`
- 运行期做 key 过滤用

3. `ruleVector`
- [pbe_compile.cpp:616](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:616)

把规则尾部最多 8 字节右对齐写进去，并做大写归一化：
```cpp
entry.ruleVector[vecIndex] = normalizedLiteralByte(c);
```

4. `tableControl`
- [pbe_compile.cpp:620](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:620)

有效字节位置置 1。

5. `tailMask`
- [pbe_compile.cpp:621](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:621)

所有有效 suffix 字节位置都置 1。

6. `headMask`
- [pbe_compile.cpp:622](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:622)

所有有效 suffix 字节中，除了最后 1 字节以外都置 1。

所以：
- `tailOnlyMask = tailMask & ~headMask`
就是“每个 slot 的最后一个有效字节”。

### 第八步：构建 L1 bitmap
- [buildPrimaryBitmap(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:165)

只要 `offsets[key] != 0`，就把位图对应位置设 1。

---

## 7. `pbeExtractPackedBitsSveBitPerm()` 是否还未实现？是不是性能关键点？

对应代码：
- [pbe_extract_sve2_bitperm.c](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c:11)
- [pbeExtractKeyBext(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:262)

**结论**
它是**部分实现**，不是完全没实现，但也绝对不是“最终性能版”。

### 已实现的部分
在满足：
- `HS_BUILD_HAVE_SVEBITPERM`
- `__ARM_FEATURE_SVE2_BITPERM`

时，它会走：

```cpp
svbext_n_u64(windowVec, mask)
```

也就是：
- [pbe_extract_sve2_bitperm.c:14](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c:14)

### 未实现 / 还很弱的部分
它当前有 3 个明显限制。

**限制 1：不是多位置并行**
当前代码是：

```cpp
const svuint64_t windowVec = svdup_n_u64(window);
```

也就是把**同一个 window** 复制到所有 lane。  
最后：
- [pbe_extract_sve2_bitperm.c:18](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c:18)

只返回 `lanes[0]`

所以它不是：
- 4 个位置一起提取
- 8 个位置一起提取

而只是：
- 对一个 `u64 window` 做一次硬件 bit extract

**限制 2：如果运行时没有 bitperm，会回退软件路径**
- [pbeRuntimeCanUseBextFastPath(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:128)
- [pbeExtractPackedBitsFallback(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:224)

所以 `mode=bext` 不等于运行时一定走了硬件 path。

**限制 3：它只优化了 key 提取，不是完整扫描器**
即使这里很快，后面还有：
- bitmap
- L1/L2 路由
- prefilter
- confirm

尤其当前真正更重的热点已经是 `confirm`。

**所以我的判断**
它当然是性能关键点之一，但**还没被实现成真正有吞吐意义的并行版本**。  
现在更准确地说是：

**“已经接上了 SVE2 bitperm 的单窗口提取能力，但还没形成多位置并行提取流水。”**

---

## 8. `pbePrimaryBitmapHasValue()` 判断位图是否有值，是否可以向量化？

对应代码：
- [pbePrimaryBitmapHasValue(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:410)
- [pbeBatchBitmapMask(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:615)

**短答**
可以做一些批量化，但“真正向量化”的收益和实现难度没有那么理想。

**为什么**
当前判断本质是：

```cpp
bitmap[idx / 8] & (1 << (idx % 8))
```

问题在于 `idx` 是当前 key，对 4 个 lane 来说：
- `idx/8` 对应的字节地址通常是离散的
- 不是连续内存

所以如果想“真向量化”：
1. 要么做 gather-like 读字节
2. 要么先软件 gather 成连续小数组
3. 再做向量逻辑

这比连续数组做 SIMD 要别扭得多。

**当前收益判断**
这块当然可以优化，例如：
1. 在 `Batch4` 里展开成 4 路无分支位测试
2. 如果 4 个 `idx/8` 落在同一个 bitmap byte，可以合并判断
3. 进一步可以做“software gather + packed test”

但它通常**不是当前最大热点**。

以你现在的性能表现看，真正更重的部分还是：
- `pbeProcessEncodedRange(...)`
- `pbeRuleExactMatch(...)`

所以这块可以优化，但我不会把它排成第一优先级。

---

## 9. L2 预筛这部分不懂，需要更详细说明每个函数和算法原理

对应代码：
- [PBEPositionContext](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:88)
- [pbeComputeValidMask8(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:165)
- [pbeBuildLaneWindow32FromWindow(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:184)
- [pbeEntryLaneMaskFromByteMatches(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:417)
- [pbeEntryMatchMaskFromContextScalar(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:445)
- [pbeEntrySingleSlotMatchMaskFromContext(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:468)
- [pbeEntryMatchMaskFromContextVector(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:502)

我按数据流讲。

### 第 1 步：构造当前输入位置的 suffix 上下文
在 `PBEPositionContext` 里缓存：
- `window64`
- `laneWindow32`
- `validMask32`

`window64`：
- 当前 `endPos` 往前最多 8 字节
- 用一个 `u64` 表示

`laneWindow32`：
- 把 `window64` 变成 8 字节 suffix 视图
- 再复制 4 份
- 形成 `32B`

对应函数：
- [pbeBuildLaneWindow32FromWindow(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:184)

也就是把：
```text
[win8]
```
复制成：
```text
[win8][win8][win8][win8]
```

这样就能和一个 L2 entry 的：
- `ruleVector[32]`
直接按字节对齐比较。

### 第 2 步：计算哪些字节在当前位置是“真实存在”的
对应函数：
- [pbeComputeValidMask8(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:165)

因为 streaming/history 场景下，当前 endPos 前面不一定真的有完整 8 字节。  
所以这个函数会算一个 8-bit mask：
- 哪些 suffix 字节是有效的
- 哪些是越界补 0 的

然后复制成：
- `validMask32`

这样预筛时不会把“根本不存在的历史字节”当真去比较。

### 第 3 步：把 entry 里 4 个 slot 的 suffix 一次性拿来比
L2 entry 结构：
- `ruleVector[32]`：4 条规则 x 每条 8 字节 suffix
- `headMask`
- `tailMask`
- `ruleCount`

当前算法并不是直接对整条规则做 confirm，而是先问：

**“这个 entry 里的哪几个 slot，看起来可能命中？”**

返回值就是：
- `laneMask`
- bit0..bit3 对应 slot0..slot3

### 第 4 步：单槽 entry 走轻量快路径
对应函数：
- [pbeEntrySingleSlotMatchMaskFromContext(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:468)

如果 `ruleCount == 1`：
- [pbeEntryMatchMaskFromContextVector(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:509)
直接走单槽快路径

算法是：
1. 先取 `tailOnlyMask = tailMask & ~headMask`
   - 也就是最后一个关键字节
2. 最后一个字节不等，直接失败
3. 再补查 `headMask` 对应的前导字节
4. 都相等才返回命中

为什么这么做：
- 单槽 entry 不值得跑完整 32B 预筛
- 先最后一个字节，再前面字节，成本更低

### 第 5 步：多槽 entry 走 laneMask 预筛
对应函数：
- [pbeEntryMatchMaskFromContextVector(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:502)

如果 `ruleCount > 1`：
1. 把 `ctx->laneWindow32` 分成两个 16B
2. 把 `entry->ruleVector` 分成两个 16B
3. 做字节相等比较
4. 得到一个 32-bit 的 `byteMatchMask`
   - 哪个字节相等，对应 bit 就是 1

然后：
- [pbeEntryLaneMaskFromByteMatches(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:417)
把这个 32-bit byte match mask 归约成 4-bit lane mask

### 第 6 步：lane mask 的归约规则是什么
对应函数：
- [pbeEntryLaneMaskFromByteMatches(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:417)

它对每个 slot 做：

1. 看这个 slot 有没有 `tailMask`
2. 最后一个字节 `tailOnlyMask` 是否全部满足
3. 头部字节 `headMask` 是否全部满足
4. 都满足才把这个 slot 加进 `laneMask`

也就是说，最终 lane 命中的判定不是“只要有一个字节相等”，而是：
- 这个 slot 的所有关键 suffix 字节都满足

### 第 7 步：通过预筛的 slot 才继续进 key / groups / confirm
对应函数：
- [pbeProcessEncodedRange(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:531)

流程是：

1. 先 decode `encoded -> offset + count`
2. 遍历这个 key 对应的连续 L2 entries
3. `laneMask = pbeEntryMatchMaskFromContextVector(...)`
4. `laneMask == 0` 就整项跳过
5. 否则只遍历命中的 slot
6. 对这些 slot 再做：
   - `((ctx->key ^ kv) & km) == 0`
   - groups
   - `pbeRuleExactMatch(...)`

### 一个重要补充
当前 L2 结构里还有：
- `tableControl[32]`

但**当前运行期预筛实际上没有直接使用它**。  
当前真正参与预筛的是：
- `ruleVector`
- `headMask`
- `tailMask`
- `validMask32`

所以 `tableControl` 现在更像：
- 结构里已经准备好了
- dump/inspect 有用
- 未来可以继续深化预筛

但它不是当前 L2 热路径的核心判断依据。

---

## 10. `PARTIAL_COVERAGE` 等“运行期禁止条件”是什么意思？为什么要有？

对应代码：
- [PBE_ARTIFACT_FLAG_PARTIAL_COVERAGE](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:13)
- [buildHashTables(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:578)
- [analyzePBEFeasibility(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:835)
- [pbeExecWithPath(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:837)

**本质含义**
它表示：

**“这套 PBE artifacts 没能完整覆盖全部规则，如果强行运行，可能会漏报。”**

这在扫描引擎里是绝对不能接受的。

### 当前会导致 partial coverage 的原因
至少包括：

1. 二级表总容量不够
- [pbe_compile.cpp:578](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:578)
- `PBE_ARTIFACT_FLAG_PARTIAL_SECONDARY_CAPACITY`

2. 某个一级 key 对应的 entryCount 超过 L1 value 可编码范围
- [pbe_compile.cpp:586](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:586)
- `PBE_ARTIFACT_FLAG_PARTIAL_ENTRY_OVERFLOW`

### 为什么运行期还要再防一次
虽然编译期：
- [analyzePBEFeasibility(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:835)
已经会拒绝这种情况

但 blob 头里仍然会把 flags 写进去：
- [buildPBEBlob(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:903)

运行期再做一次：
- [pbeExecWithPath(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:852)

```cpp
if (!a || (hdr->flags & PBE_RUNTIME_FLAG_PARTIAL_COVERAGE)) {
    return HWLM_SUCCESS;
}
```

这是一个**最后保险丝**。

意义是：
1. 编译期理论上不该放进来
2. 但如果真的放进来了，运行期宁可不扫，也不能产生不完整结果

**所以它不是“功能条件”，而是“正确性保护条件”**

---

## 11. `PBECompileArtifacts` 里的 `windowBytes` 为什么是 8？可以调整吗？

对应代码：
- [pbe_compile.h:79](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:79)
- [pbe_compile.cpp:132](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:132)
- [pbe_runtime.h:41](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_runtime.h:41)

**为什么是 8**
因为当前整个 PBE 设计就是围绕“**尾部 8 字节窗口**”建立的。

这 8 的来源不是随便取的，而是整个结构都绑死在它上面：

1. selector 候选空间是 8 字节 = 64 bit
- [pbe_compile.cpp:29](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:29)

2. `window64` 是 `u64`
- [pbe_engine.c:141](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:141)

3. 每个 L2 slot 只存 8 字节 suffix
- [pbe_compile.h:21](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:21)

4. `ruleVector/tableControl`
- 4 条规则 x 8 字节 = 32B

5. `headMask/tailMask`
- 也是按 32 个 byte lane 来编码的

6. `bextMask`
- 用 `u64a`，也正好覆盖 64 bit

**当前它能不能调**
严格说：
- **字段上可以存别的值**
- 但**当前实现并没有真正把它做成可配置参数**

因为运行期主路径里实际上还是写死按 8 来跑的，例如：
- [pbeBuildPositionContext(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:286)

这里调用的是：

```cpp
pbeLoadWindow64Normalized(..., PBE_RUNTIME_BYTES_PER_RULE_SLOT)
```

不是 `hdr->windowBytes`

所以当前 `windowBytes` 更多是：
1. 序列化字段
2. 布局描述
3. 未来扩展预留

**而不是当前真的可调的 runtime knob**

**结论**
现在不能把它当成一个“改个值就能生效”的参数。  
如果你真要改成 4、16 或别的大小，需要连带改：

1. selector 选择空间
2. `window64` 类型
3. `bextMask`
4. L2 slot 大小
5. `ruleVector/tableControl`
6. `headMask/tailMask`
7. prefilter / confirm / tests

所以这本质上是一个结构 redesign，不是参数微调。

---

## 12. 运行期 `exactEncoded` 和 `wildcardEncoded` 分别表示什么？

对应代码：
- [PBEPositionContext](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:88)
- [pbeBuildPositionContext(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:281)
- [pbeRunNaive(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:708)
- [pbeRunBatch4(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:766)
- [pbeDecodePrimaryValue(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:828)

**先说定义**
它们都是一级哈希表里的 `u32` 编码值。

编码格式：
```text
low 18 bits  = secondaryOffset
high bits    = entryCount
```

用：
- [pbeDecodePrimaryValue(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:828)
来解码。

### `exactEncoded`
表示：

**当前 `endPos` 提取出的 key，对应的一级哈希 exact bucket 的编码值**

也就是：
1. 先用当前输入位置算出 `ctx->key`
2. `ctx->primaryIdx = key`
3. 如果 bitmap 表示这个 key 非空
4. `exactEncoded = primaryHashTable[key]`
5. 否则为 0

### `wildcardEncoded`
表示：

**一级哈希表里 key=0 这个 wildcard bucket 的编码值**

这个 bucket 存的不是“key 真的是 0 的 exact 规则”，而是：

**所有 `keyMask != fullMask` 的规则**

对应编译期逻辑：
- [pbe_compile.cpp:562](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:562)

```cpp
bucketKey = (keyMask == fullMask) ? keyValue : 0U;
```

也就是说：
- exact 规则 -> 各自 exact key
- partial / don't-care 规则 -> 全部进 `key=0`

### 运行期怎么用
当前运行期顺序是：

1. 先处理 `exactEncoded`
2. 再处理 `wildcardEncoded`
3. 如果二者相同，就避免重复处理

对应：
- [pbeRunNaive(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:749)
- [pbeRunBatch4(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:803)

**举你 inspect 里的例子**
你那个 6 条规则例子里：
- `alpha`
- `delta`
- `gamma`
是 exact bucket
- `ALPHA`
- `beta`
- `THETA`
在 wildcard bucket

所以对某个具体位置：
- `exactEncoded` 可能指向 `L2[2]`、`L2[3]`、`L2[4]` 某一个
- `wildcardEncoded` 则固定指向 `L2[1]`

所以这两个字段的语义就是：

1. `exactEncoded`
   - “当前 key 的精确二级入口范围”

2. `wildcardEncoded`
   - “全局 key=0 的模糊二级入口范围”

---

## 我对这 12 个问题的总体结论

最值得你继续盯住的几个点是：

1. `choosePbeEngine` 现在确实主要是**借用 Neo 外层描述逻辑**
   - 不错，但不够纯
2. `100000000` 规则上限现在**结构上不成立**
   - 不能只改前置条件
3. `bextToKeyBit` 的设计是合理的
   - 它是 `packed bit -> key bit` 的升序 source-bit 重映射
4. `buildPrimaryBitmap()` 是**固定大小 dense bitmap**
   - 不是 sparse list
5. `pbeExtractPackedBitsSveBitPerm()` 是**半成品**
   - 有硬件路径，但还没实现多位置并行
6. 当前真正的大热点已经更偏向：
   - `exact confirm`
   - 极短规则集下的整体结构不适配

如果你愿意，下一步我可以先给一个**详细修改计划**，专门处理你刚才最关键的两个后续方向之一：

1. **把规则数上限改造到真正可扩展**
2. **把 exact confirm 做成短规则快路径，继续收性能**

你选一个方向，我先出计划，不直接改代码。

# 4.1 问题答复
我把这 7 个问题再往深一层收了一遍。先给一个总判断，方便我们对齐：

1. 你抓到的几个点都对，尤其是 `single-key`、`bextToKeyBit`、`wildcardEncoded` 这三块，确实是当前 PBE 设计里最值得重新审视的地方。
2. 当前实现“能跑、结果对”，但很多地方还是**为了先接进现有 FDR/Neo 框架而做的过渡设计**。
3. 如果目标从“先打通”转成“真正追性能”，那现在最该动的不是再补小修小补，而是**重新整理 key 编码和 masked-rule 路由策略**。

下面我逐条回答。

---

## 1. 当前 PBE 外层 proto/bucket 选择借用 Neo/FDR，这里是否有冗余信息，实际运行期并没有用到？

对应代码：
- [fdrBuildProtoInternal](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_compile.cpp:878)
- [chooseNeoFdrEngine](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_engine_description.cpp:214)
- [choosePbeEngine](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_engine_description.cpp:319)

**结论**
有，而且不少。

### 当前外层这段到底在做什么
`choosePbeEngine(...)` / `chooseNeoFdrEngine(...)` 选出来的是一个 `FDREngineDescription`，随后用于：

1. `assignStringsToBuckets(...)`
2. `addIncludedInfo(...)`
3. 构造 `HWLMProto`
4. 继续走现有 `FDRCompiler` / Rose / HWLM 编译大链路

也就是说，它服务的是**外围编译框架兼容性**，不是 PBE 自己的 22-bit L1/L2 哈希结构。

### 对 PBE 运行期真正有用的是什么
PBE 运行期真正读的是：
- [PBERuntimeHeader](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_runtime.h:29)
- `selectors / bextMask / bextToKeyBit`
- `primaryBitmap`
- `primaryHashTable`
- `secondaryHashTable`
- `ruleMeta`
- `literalBlob`

而不是 `FDREngineDescription.bits/stride` 这一套。

### 所以冗余在哪里
从 **PBE 运行期视角** 看，下面这些基本都是外层壳信息：

1. `FDREngineDescription.bits`
2. `FDREngineDescription.stride`
3. `bucketToLits`
4. Neo/FDR 风格的 bucket 切分策略本身

### 但为什么现在还不能直接删
因为当前 PBE 不是一个完全独立的编译管线，而是**挂在现有 FDR/Neo 生态里**。这些信息仍然参与：

1. `HWLMProto` 的统一构建
2. `addIncludedInfo(...)`
3. `FDRCompiler` 的整体编译约定
4. Rose 程序 id / 偏移写回后的后续流程

### 我的判断
你可以把这层理解成：

- **PBE 外层 proto 仍借用了 Neo/FDR 的编译框架**
- **PBE 内层扫描结构才是自己的**

所以答案是：
- **有冗余**
- **运行期确实不用**
- **但当前为了复用既有编译链路，这层壳暂时还不能完全拿掉**

如果后面真要把 PBE 做成更“纯”的独立路径，这一层是值得瘦身的。

---

## 2. 规则数上限现在是 65535，那么什么情况下很多规则会落到同一个 key？如果冲突不多，上限是多少？能否修改 65535？同时评估内存占用

对应代码：
- [analyzePBEFeasibility](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:771)
- [当前全局上限判断](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:808)
- [ruleIndex 是 u16](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:54)
- [L1 count|offset 编码](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:17)

### 先说两个“上限”
当前其实有两个不同层次的上限：

1. **全局规则数上限**
- 由 `ruleIndex` 的 `u16` 决定
- 当前最大规则数是 `65535`

2. **单个 key 的局部上限**
- 由 L1 value 里的 `count` 位宽决定
- 当前低 18 bit 是 offset，高 14 bit 是 entryCount
- 每个 key 最多 `16383` 个 L2 entry
- 每个 entry 4 条规则
- 所以单 key 最多 `65532` 条规则

### 什么情况下很多规则会落到同一个 key
这里最容易误判的一点是：

**“冲突多”不一定是因为 hash bit 选得差，也可能是因为当前 single-key 策略把很多 partial-mask 规则强行压进了同一个 bucket。**

当前关键逻辑：
- [buildHashTables](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:562)

```cpp
const u32 bucketKey = (keyMask == fullMask) ? keyValue : 0U;
```

也就是说：

1. `keyMask == fullMask`
- 这条规则对所有被选中的 22 个 bit 都是 cared
- 才能进 exact key

2. `keyMask != fullMask`
- 只要有一个 bit 是 don't-care
- 就直接进 `key=0` 的 wildcard bucket

### 所以“很多规则落到同一个 key”的典型场景
1. 很多短规则
- 选中的 bit 如果落在规则长度之外，这些 bit 就是 don't-care

2. 很多 `nocase` 规则
- 对 ASCII 字母，大小写位会被视为 don't-care

3. 有 `msk/cmp` 的规则
- 某些位并没有被真正约束

4. selector 选到了区分度不够高、或者大量规则共享相同 suffix bit 的位置

5. 大量规则都属于 partial-mask
- 在当前 single-key 策略下，它们会**全部堆到 `key=0`**

所以你问“如果哈希 bit 取得较为合理，冲突没那么多，会怎样”：
- 对 **full-mask exact 规则**，确实冲突会明显变少
- 但对 **partial-mask 规则**，当前实现里它们还是会直接堆到 wildcard bucket，不受“bit 选得合理”这件事的完整保护

### 如果冲突不多，现在的规则上限是多少
**在当前代码下，仍然还是 65535。**

原因不是哈希冲突，而是：
- [ruleIndex 是 `u16`](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:54)

所以：
- **冲突少时，单-key 65532 这个局部上限可能不会先触发**
- 但**全局 65535 这个类型上限仍然会先触发**

### 能不能修改 65535
能，但不是只改前置判断。

至少要改：

1. `ruleIndex[u16] -> u32`
- [pbe_compile.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h:51)
- [pbe_runtime.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_runtime.h:58)

2. 重新评估 L1 `count|offset` 编码
- 如果单 key 可能远超 65532，14 bit count 也会先炸

3. 重新评估 `litOffset` / `literalBlobSize`
- 现在还是 `u32`

### 当前内存占用怎么估算
先给你一个更准确的近似公式。

#### 固定成本
1. L1 主表
- `2^22 * 4B = 16,777,216B`
- 约 `16 MiB`

2. L1 bitmap
- `2^22 bit = 524,288B`
- 约 `512 KiB`

#### 变量成本
1. `PBERuntimeSecondaryHashEntry`
当前字段总和约：
- `32 + 32 + 8 + 16 + 16 + 4 + 4 + 2 + 2 = 116B`

所以：
- 每个 L2 entry 约 `116B`

2. `PBERuntimeRuleMeta`
当前大约是 `56B` 级别

3. `literalBlob`
- 约 `平均规则长度 * N`

### L2 大小有两个极端
这是很关键的一点。

#### 情况 A：冲突很低，很多 key 各自独占
- 每个 key 可能只有 1 条规则
- 但每个 key 仍要占 1 个 L2 entry
- 所以 `L2 entries ≈ N`

#### 情况 B：规则被高度打包到同一 key
- 每 4 条规则占 1 个 entry
- `L2 entries ≈ ceil(N/4)`

### 所以总占用大概是

#### 低冲突场景
```text
总大小 ≈ 16.5MB + N * (116B + 56B + avgLen)
       ≈ 16.5MB + N * (172B + avgLen)
```

#### 高打包场景
```text
总大小 ≈ 16.5MB + N * (29B + 56B + avgLen)
       ≈ 16.5MB + N * (85B + avgLen)
```

### 举例：N = 65535
假设平均规则长度 `avgLen = 1`

#### 低冲突
```text
≈ 16.5MB + 65535 * 173B
≈ 16.5MB + 10.8MB
≈ 27.3MB
```

#### 高打包
```text
≈ 16.5MB + 65535 * 86B
≈ 16.5MB + 5.4MB
≈ 21.9MB
```

这还没算：
- `std::map`
- `litKeyValue/litKeyMask`
- vector 扩容
- 构建期临时对象

所以编译期峰值会更高，这和你 hsbench 看到的几百 MB peak heap 是一致的。

### 我的结论
1. 当前 `65535` 是**全局类型上限**，不是“哈希冲突估出来的经验值”
2. 即使冲突很低，当前上限也还是 `65535`
3. 如果要改这个上限，第一步应当是：
   - `ruleIndex -> u32`
4. 但如果不先重新设计 masked-rule 路由，单 key overflow 和内存压力仍然会很难看

---

## 3. `bextToKeyBit` 的设计是否合理？结合 Arm 的 BEXT 指令，这里是不是有问题，需要调整？

对应代码：
- [buildExtractDescriptor](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:125)
- [pbeExtractKeyBext](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:262)
- [pbeRemapPackedBits](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:240)
- [pbeExtractPackedBitsSveBitPerm](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c:11)

我先直接给结论：

### 结论
**当前设计在语义上是成立的，但从硬件 `BEXT` 的使用方式看，不是最优的，确实应该调整。**

### 先说 Arm `BEXT` 的语义
结合你给的 Arm ISA 页，以及 Arm 官方社区文章，`BEXT` 的核心语义是：

1. 用第二个参数里的 bitmask 选择源位
2. 把被选中的位“压缩”到结果的低位
3. 压缩顺序是按源 bit 位置从低到高
4. 结果落在向量寄存器里
5. 对 SVE 来说，它是按元素宽度独立工作的；对我们这里的 `u64` 场景，本质上就是按 64-bit lane 独立提取

Arm 官方社区这篇文章对它的描述很明确：
- `BEXT` “can collect arbitrary input bits ... controlled by a second argument”
- 并且对 128-bit / 64-bit halves 的行为做了说明  
链接见文末。

### 当前实现为什么要有 `bextToKeyBit`
因为当前编译期有两个顺序：

1. **selector 的逻辑顺序**
- key bit 0..21 是按 `selectors` 向量顺序定义的

2. **BEXT 的 packed 顺序**
- 按 source bit index 升序压缩

所以当前做法是：

1. 先把 `(sourceBitIndex, selectorIndex)` 组成 pair
2. 按 `sourceBitIndex` 升序排序
3. `bextMask` 用排序后的 source bit 填
4. `bextToKeyBit[i] = 原始 selectorIndex`

这样 runtime 可以：
1. 先 `bext(window, mask)` 得到 packed bits
2. 再通过 `bextToKeyBit` 回填到逻辑 key 位

### 这是不是“逻辑错误”
不是逻辑错误。  
如果 `BEXT` 的 packed 语义是“按 source bit 升序压缩”，那当前实现是**语义正确**的。

这一点也和当前软件 fallback 一致：
- [pbeExtractPackedBitsFallback](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:224)

它就是按最低置位 bit 不断弹出，也就是 source bit 升序。

### 但你指出的问题也对
如果目标是**为硬件 BEXT 优化而设计 key 编码**，那当前这层 remap 确实是多余成本。

换句话说：

- 当前方案是：  
  “保留旧的 selector 逻辑顺序，再用 `bextToKeyBit` 修正”
- 更合理的优化方案应该是：  
  “让 compile-time 的 key 位顺序，直接等于 BEXT 的 packed 顺序”

### 我认为更好的设计
如果真按 `BEXT` 作为第一公民来设计，应该这样改：

1. selector 选出来之后，不再保留“原始选择顺序”作为 key bit 顺序
2. 直接按 `sourceBitIndex` 升序重排 selectors
3. 后续 compile-time 的：
   - `keyValue`
   - `keyMask`
   - `bucketKey`
都按这个重排后的顺序生成
4. runtime 直接：
   - `key = BEXT(window, mask)`
5. 去掉：
   - `bextToKeyBit`
   - `pbeRemapPackedBits(...)`

### 这样做的好处
1. key 提取真正变成“一条硬件语义直接产出最终 key”
2. 没有额外 remap
3. 更符合你最初说的：
   - “编译期计算 mask”
   - “运行期根据 mask 一次性提取 key”

### 现在 `svbext` 的使用还有另一个问题
当前实现：
- [pbe_extract_sve2_bitperm.c](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c:13)

```c
const svuint64_t windowVec = svdup_n_u64(window);
const svuint64_t packedVec = svbext_n_u64(windowVec, mask);
...
return lanes[0];
```

这说明它现在只是：
- 把**一个** `window` 广播到所有 lane
- 做一次 `BEXT`
- 最后只取 lane0

所以它并没有真正利用 SVE 的**多 lane 并行性**。

### 我的结论
1. 当前 `bextToKeyBit` **语义正确**
2. 但对于性能目标来说，**设计不够理想**
3. 我同意你的判断：  
   **应该把 compile-time key bit 顺序改成与 BEXT packed 顺序一致，从而去掉 remap**

这会是一个很重要的重构点。

---

## 4. bitmap 运行期现在还是逐位检查，效率太低，需要向量并行化

对应代码：
- [pbePrimaryBitmapHasValue](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:410)
- [pbeBatchBitmapMask](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:615)

我先温和纠正一下表述：

### 当前不是“扫描整个 bitmap”
它现在不是把 524288 字节 bitmap 从头扫到尾，而是：

- 对每个 lane 的 `primaryIdx`
- 做一次定位：
  - `bitmap[idx / 8]`
  - `1 << (idx % 8)`

也就是**逐 lane bit-test**，不是遍历整个位图。

### 但你的结论仍然成立
当前这块确实还是偏标量，尤其在：
- `Batch4`
- 或未来更宽 batch

里，收益会越来越不够。

### 为什么现在还没有真正向量化
因为 bitmap 查询的难点不在 bit 操作本身，而在：

**每个 lane 的 `idx/8` 通常是离散地址**

也就是说，这不是连续内存 load，而更像：
- gather byte load
- 然后每 lane 再做 bit-test

### 我的判断
这块可以优化，而且应该优化，但前提是：

1. batch 宽度不能还是 4
2. 最好已经进入真正的 SVE/VL-dependent 批处理
3. 能把：
   - `byteIndex = idx >> 3`
   - `bitMask = 1 << (idx & 7)`
   变成按 lane 的向量数据流

### 比较合理的实现方向
1. 一批生成多个 `primaryIdx`
2. 向量化算：
   - `byteIndex`
   - `bitShift`
3. gather bitmap byte
4. `bitmapByte & (1 << bitShift)`
5. 形成 active mask
6. 再 `compact`

### 但我会强调一点
从你现在的 `hsbench` 表现看，bitmap 这块**不是第一大瓶颈**。  
它值得做，但还不是最关键的第一优先级。

现在更重的还是：
1. masked-rule 路由设计
2. exact confirm
3. 多位置真正并行的 `BEXT`

---

## 5. 为什么构建哈希表时会有 `single-key` 策略？`care` 表示什么？实际规则集不应该直接按选中的 bit 提取规则哈希值吗？这里是不是需要重新思考框架设计？

对应代码：
- [getBitState](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:353)
- [computeKeyValueMaskForLiteral](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:45)
- [single-key bucket 分配](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:562)

### 先解释 `care`
`care` 的意思是：

**对于当前被选中的某个 bit，这条规则是否真的约束了这个 bit 的取值。**

如果约束了：
- 这个 bit 是 `care`
- 它对 key 有贡献

如果没有约束：
- 这个 bit 是 `don't-care`
- 它不应该参与 exact key 匹配

### 什么情况下会变成 don't-care
1. 规则太短，这个 byte 根本不存在
2. `nocase` 下，字母的大小写 bit 不关心
3. `msk/cmp` 没有约束这个 bit
4. 其他导致该 bit 不是确定 0/1 的情况

### 你说“规则集构建不应该直接按选择的 bit 提取规则哈希值吗”
这个说法对 **full-care 规则** 完全成立。  
但对 **partial-care 规则** 不成立。

因为这时候这条规则对应的不是一个单一 key，而是：

**一个 key 空间里的集合 / 超立方体**

举个简单例子：
- 选了 4 个 bit
- 其中 2 个 bit 是 don't-care

那这条规则对应的不是 1 个 key，而是：
- `2^2 = 4` 个可能 key

所以这里真正的设计问题不是“为什么不直接哈希”，而是：

**“一条带 don't-care 的规则，应该如何映射到 key 空间？”**

### 当前 single-key 策略为什么会存在
因为这是最简单的实现：

1. full-care 规则
- 用 exact key

2. 只要有 don't-care
- 全部压到 `key=0`

优点：
1. 编译期简单
2. 不会指数展开
3. 运行期实现最容易先打通

缺点：
1. wildcard bucket 很容易爆
2. 大量 masked/nocase/短规则会失去 hash 区分度
3. 运行期成本很高

### 所以你这个判断是对的
这里确实应该重新思考框架。

### 更合理的设计方向
我认为至少有三种可选设计：

#### 方案 A：纯 exact key 展开
对于 `d` 个 don't-care bit 的规则：
- 展开成 `2^d` 个 exact keys

优点：
- runtime 纯 exact 查表最快
- `wildcardEncoded` 可以消失

缺点：
- `d` 稍大就爆炸

#### 方案 B：当前 single-key wildcard
优点：
- 最简单

缺点：
- runtime 太差

#### 方案 C：混合策略，我认为最合理
1. 如果 `d <= T`
- 小规模展开成多个 exact keys

2. 如果 `d > T`
- 进入 residual/wildcard 路径

这样可以：
1. 避免指数爆炸
2. 又显著减少 wildcard 压力

### 我的结论
1. 当前 single-key 策略是“为了先跑通”的简化版
2. 它不是最终合理设计
3. 你说得对，这里应该重新思考
4. 如果要真做性能版，我最推荐的是：
   - **小 don't-care 展开**
   - **大 don't-care 保留 residual bucket**
   - 而不是继续维持“全扔 key=0”

---

## 6. `pbeExtractPackedBitsSveBitPerm()` 还需要进一步优化成多位置并行提取流水

对应代码：
- [pbe_extract_sve2_bitperm.c](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c:11)
- [pbeExtractKeyBext](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:262)

**是的，完全同意。**

### 当前问题
当前函数只是：
1. 单个 `window`
2. 广播到所有 lane
3. 做 `svbext`
4. 取 lane0

所以本质上只是：
- “单窗口硬件 bit extract helper”

不是：
- “多位置并行提取流水”

### 你真正想要的流水应该是什么
更接近下面这条链：

1. 一批 `window64`
2. 打包成 SVE `u64` lanes
3. `svbext(mask)` 对所有 lanes 一次提取
4. 得到一批 `key`
5. 后续继续一批做 bitmap / L1 / compact

### 这一步要成立的前提
1. key 位顺序最好已经和 BEXT packed 顺序一致
- 也就是前面第 3 点说的 redesign

2. Batch 宽度不能固定死 4
- 应该转成更自然的 VL-dependent 宽度

3. bitmap / L1 路由也要一起向量化
- 否则前面提了一批 key，后面马上又散掉了

### 所以这件事非常值得做
而且从性能上看，它比继续修补小逻辑更值。

---

## 7. 如果构建哈希表时不使用 `single-key` 策略，是不是就用不到 `wildcardEncoded` 了？

对应代码：
- [PBEPositionContext](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:88)
- [wildcardEncoded 使用](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c:730)
- [wildcard key=0 路由](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp:565)

### 短答
**不一定。**

### 情况 1：如果你改成“纯 exact key 展开”
也就是所有 partial-mask 规则都展开到全部具体 key：

- 那确实可以不要 `wildcardEncoded`
- 因为每条规则都已经精确挂到某些 exact key 上了

### 情况 2：如果你改成“混合策略”
比如：
- `d <= T` 展开
- `d > T` 留在 residual bucket

那你仍然需要一个类似 `wildcardEncoded` 的东西。

只是那时它更准确的名字可能不该叫 `wildcardEncoded`，而应该更像：
- `residualEncoded`
- `maskedFallbackEncoded`

因为它表示的是：
- “没有完全 exact 化的剩余规则范围”

### 我的建议
如果后面重构：
1. 如果做纯 exact 化
- 可以删掉 `wildcardEncoded`

2. 如果做工程上更现实的混合方案
- 不建议删
- 但建议改语义、改命名

---

## 我对这 7 个问题的最终判断

最重要的三条是：

1. **外层 proto/bucket 借壳确实有冗余**
- 运行期不直接用
- 但当前还承担框架兼容作用

2. **`bextToKeyBit` 语义没错，但设计上不够硬件友好**
- 这一点我同意你
- 更好的方案是让 compile-time key 位顺序直接等于 BEXT packed 顺序

3. **single-key wildcard 是当前性能问题的核心源头之一**
- 它是简化设计，不是最终设计
- 真要追性能，这里必须重构

**如果按优先级排后续设计改造，我建议是：**
1. 先重做 key 位顺序，让 `BEXT` 直接产出最终 key
2. 再重做 masked-rule 路由，弱化或替代 current wildcard bucket
3. 再做真正的多位置 SVE2 `BEXT` 提取流水
4. 最后再把 bitmap / L1 路由向量化吃满

**我参考的源码**
- [fdr_compile.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_compile.cpp)
- [fdr_engine_description.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/fdr_engine_description.cpp)
- [pbe_compile.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h)
- [pbe_compile.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp)
- [pbe_runtime.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_runtime.h)
- [pbe_engine.c](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c)
- [pbe_extract_sve2_bitperm.c](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c)

**我参考的官方资料**
- 你给的 Arm ISA 页面：`https://developer.arm.com/documentation/ddi0602/2025-06/SVE-Instructions/BEXT--Gather-lower-bits-from-positions-selected-by-bitmask-`
- Arm 官方社区文章，对 `BEXT` 的行为有很清晰的解释：  
  https://developer.arm.com/community/arm-community-blogs/b/architectures-and-processors-blog/posts/webassembly-bitmask-operations

如果你愿意，下一步我可以先不给代码，先出一份**详细重构计划**，专门解决这两个核心问题：

1. **去掉 `bextToKeyBit` 的 remap，让 `BEXT` 直接生成最终 key**
2. **重做 single-key / wildcard 策略，改成混合 exact-key 展开方案**

我会先把方案拆到结构体、函数、兼容性和 UT 级别，再由你确认后实施。

# 4.1 架构整改计划

可以，这三个重点的顺序我认同，而且它们之间的依赖关系也比较合理。  
这一步我先不给代码，先把**分阶段详细修改计划**列清楚，等你确认后我再按阶段实施。

---

**总目标**

按你给的顺序，把当前 PBE 从“能跑的框架版”往“真正围绕 BEXT/位图/按位哈希设计的实现”重构：

1. **重构 `bext` 逻辑**
   - 让 `BEXT` 真正成为 key 提取主路径
   - 不再保留当前这套不够自然的 remap 设计

2. **重构 bitmap 路径**
   - 把 bitmap 变成运行期前端核心加速点
   - 做批量并行查询 + compact

3. **重构规则集哈希构建**
   - 不再沿用当前 single-key / wildcard 0 桶策略
   - 不再依赖传统 FDR 风格 bucket 语义
   - 直接基于“选择出的 bit”构建 PBE 自己的哈希组织

---

# 第一阶段：重构 `BEXT` 逻辑，使用 `BEXT` 向量化指令实现

## 目标
把当前：
- `window64 -> bext(mask) -> packed bits -> bextToKeyBit remap -> key`

改成更自然的：
- **编译期直接把 key bit 顺序定义成 `BEXT` packed 顺序**
- 运行期 `BEXT` 一步直接得到最终 key
- 并且开始支持**多位置并行提取**

---

## 当前问题
当前实现虽然已经有：
- `bextMask`
- `bextToKeyBit`
- `pbeExtractPackedBitsSveBitPerm(...)`

但还存在 4 个问题：

1. `bextToKeyBit` 是额外 remap 成本
2. 编译期 key 位顺序和 `BEXT` packed 顺序不一致
3. `SVEBITPERM` 路径现在还是“单窗口提取”，不是多位置流水
4. `mode=bext` 只是框架接通，不代表已经进入最终高性能形态

---

## 第一阶段修改方案

### 1. 编译期重排 selector 顺序
涉及文件：
- [pbe_compile.cpp](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp)
- [pbe_compile.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.h)
- [pbe_runtime.h](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_runtime.h)

修改思路：
1. 保留当前 selector 选择算法
2. 但在最终确定 selectors 后，**按 source bit index 升序重排**
3. 后续 compile-time 的：
   - `keyValue`
   - `keyMask`
   - `bucketKey`
   - `ruleMeta/key info`
   全部使用这个重排后的 key 位顺序

结果：
- `BEXT(window, mask)` 输出的 packed bits 顺序
- 直接等于最终 key 位顺序

### 2. 去掉 `bextToKeyBit`
修改思路：
1. 删除 compile-time `bextToKeyBit`
2. 删除 runtime `bextToKeyBit`
3. 删除：
   - `pbeRemapPackedBits(...)`
4. `pbeExtractKeyBext(...)` 直接返回 packed result 的低 `keyBits`

这样：
- `BEXT` 不再是“中间结果”
- 而是“最终 key 生成器”

### 3. 重构 `buildExtractDescriptor(...)`
当前：
- 既生成 `bextMask`
- 又生成 `bextToKeyBit`

修改后：
- 只生成：
  - `bextMask`
  - `extractMode`
  - `windowBytes`
- `bextMask` 中 bit=1 的位置，就是最终要抽取的 source bit
- 不再需要 remap 表

### 4. 重构 runtime key 提取路径
涉及文件：
- [pbe_engine.c](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c)
- [pbe_extract_sve2_bitperm.c](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_extract_sve2_bitperm.c)

修改思路：
1. `pbeExtractKeyBext(...)`
   - 直接返回 `BEXT(window, mask)` 的结果
2. 保留 scalar fallback
   - 但 scalar 也要按重排后的 selector 顺序生成 key
3. scalar / bext 必须完全一致

### 5. 把 `SVEBITPERM` helper 改成真正的批量接口
当前：
- 单个 `window` 输入
- 只返回 lane0

修改后我建议新增：
- `pbeExtractPackedBitsSveBitPermBatch(...)`

接口形态建议：
- 输入：
  - `const u64a *windows`
  - `u32 count`
  - `u64a mask`
- 输出：
  - `u32 *keys`

第一版目标：
1. 支持一批 `4/8/16` 个 `window64`
2. 先做真正的 lane 并行提取
3. 再结合后续 bitmap 阶段使用

### 6. Batch4 先升级为“批量提取 key”
当前 `Batch4` 只是前端组织批处理。  
第一阶段完成后，至少要让：

- `pbeBuildBatch4Contexts(...)`

在 key 提取阶段真正使用：
- 批量 `SVEBITPERM BEXT`

而不是每个 lane 独立调一次单窗口 helper。

---

## 第一阶段测试计划

### 单测新增/调整
文件：
- [pbe_vs_neo.cpp](d:/work/2026/Hyperscan/code/hyperscan/unit/internal/pbe_vs_neo.cpp)

新增：
1. `PBEExtract.BextDirectKeyMatchesScalarAfterSelectorReorder`
2. `PBEExtract.BextBatchMatchesScalarBatch`
3. `PBECompile.SelectorOrderMatchesBextPackedOrder`
4. `PBECompile.NoRemapTableNeededAfterReorder`

保留并继续通过：
- `PBEExtract.*`
- `PBECompile.*`
- `PBEvsNeo.*`
- `PBERuntime.*`

---

## 第一阶段文档更新
涉及：
- [PBE_Engine_System_Analysis_Current.md](d:/work/2026/Hyperscan/code/hyperscan/documents/PBE_Engine_System_Analysis_Current.md)
- [PBE_Design_Compile_Runtime.md](d:/work/2026/Hyperscan/code/hyperscan/documents/PBE_Design_Compile_Runtime.md)
- [PBE_Implementation_Report_Current.md](d:/work/2026/Hyperscan/code/hyperscan/documents/PBE_Implementation_Report_Current.md)

补充：
1. `BEXT` 的 packed 顺序说明
2. 为什么移除 `bextToKeyBit`
3. 新的 `window64 -> BEXT -> final key` 数据流图

---

# 第二阶段：bitmap 优化，作为运行期前端核心

## 目标
把 bitmap 从“逐 lane bit-test 辅助逻辑”升级为真正的**批量前端过滤器**。

---

## 当前问题
当前 bitmap 查询：
- [pbePrimaryBitmapHasValue(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c)
- [pbeBatchBitmapMask(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_engine.c)

本质上还是：
1. 每个 key 算 `idx/8`
2. 每个 key 读一个 bitmap byte
3. 再按位判断

这还不是你要的：
- 批量并行
- 查位图
- compact
- 只对非零单元继续访问 L1

---

## 第二阶段修改方案

### 1. 把 Batch4 升级成可扩展 batch
建议先把当前固定：
- `PBE_BATCH_WIDTH = 4`

抽象成：
- 逻辑 batch 宽度
- 未来允许按 SVE vector length 提升

第一步不强制改成 VL 变量，但代码结构要为 VL 留口。

### 2. 批量计算 bitmap 访问信息
对一批 key：
1. 计算 `byteIndex = key >> 3`
2. 计算 `bitMask = 1 << (key & 7)`
3. 批量取 bitmap bytes
4. `bitmapByte & bitMask`
5. 得到 active lanes

### 3. 加入真正的 compact
compact 不是现在这种“简单收集非空 lane 下标”就结束，而是明确分三步：

1. 批量 bitmap probe
2. 形成 lane activity mask
3. 压紧：
   - `key`
   - `primaryIdx`
   - `endPos`
   - `window64`
   - `context index`

这样后面 exact/wildcard/L1 路径就只处理非空项。

### 4. packed L1 load 继续前推
在 compact 后：
1. 批量读取 L1 value
2. 形成 packed 的 `(offset,count)` 数组
3. 再交给后续 L2 处理

这一步仍然允许先用 software gather + packed array 的方式，不强行一步到硬件 gather。

### 5. 为后续 exact/wildcard 双路保序回放保留约束
这一阶段仍然必须保持：
1. 同一位置 exact 先于 wildcard
2. 全局 callback offset 单调不降

因为前面我们已经验证过，Rose 对顺序非常敏感。

---

## 第二阶段测试计划

新增：
1. `PBERuntime.BitmapBatchMatchesScalar`
2. `PBERuntime.BitmapCompactKeepsOrder`
3. `PBERuntime.BitmapCompactSkipsEmptyLanesOnly`
4. `PBERuntime.BitmapBatchExactWildcardOrderStable`

---

## 第二阶段文档更新
补充：
1. bitmap 的固定布局
2. 批量 bitmap probe 数据流
3. compact 为什么是性能关键点

---

# 第三阶段：重构规则集构建，按选择的 bit 直接构建 PBE 哈希，不再沿用当前 single-key / wildcard 设计

这是最关键的一阶段，也是对当前框架影响最大的一阶段。

---

## 目标
实现你说的这件事：

**规则集构建直接按选择的 bit 提取规则哈希值，不再沿用传统 FDR 风格外层哈希语义，不再采用当前 single-key / wildcard 0 桶策略。**

同时满足：
1. 不做暴力规则展开
2. 不把所有 partial-mask 规则全扔进 wildcard 0 桶
3. 让运行期能直接使用 `BEXT` 提取值进行路由

---

## 我建议的重构方向：按 `keyMask` 类分层建表

这是我觉得最适合你目标的一种方案。

### 核心思想
不再把所有 partial-mask 规则都压进一个 `key=0` bucket。  
而是：

1. 每条规则仍然有：
   - `keyValue`
   - `keyMask`

2. 先按 `keyMask` 分组
   - 也就是按“这条规则到底 care 哪些 bit”分成不同 mask class

3. 对每一个 `mask class`
   - 构建它自己的 key 域
   - key 不是完整 22 位，而是 `popcount(mask)` 位的 packed key

4. 运行期：
   - 对输入窗口执行一次 full-key BEXT，或按 class mask 执行 class-key BEXT
   - 再去对应 class table 查找

### 为什么这比 single-key/wildcard 更合理
因为：
- exact 全 cared 规则
  - 仍然走 full-key table
- partial cared 规则
  - 不需要展开
  - 也不需要全部塞到 wildcard 0
  - 只需要进它所属的 mask class table

### 举个例子
如果有一类规则的：
- `keyMask = 0b111011...`

说明它只 care 其中 17 个 bit。  
那就为这类规则建立一个：
- 17-bit key table

规则只需要落入这个 table 的一个 key：
- `projectedKey = project(keyValue, keyMask)`

运行期同样：
- `projectedInputKey = project(inputKey, keyMask)`

这样：
1. 不需要展开成多个 exact key
2. 不需要 wildcard bucket
3. 仍然是“按选中的 bit 直接提取哈希值”

### 这和 BEXT 非常匹配
因为 `BEXT` 本质上就是：
- 按 mask 选 bit
- 压缩成 packed result

所以对于每个 mask class，运行期可以直接做：

```text
projectedKey = BEXT(window64, classMask)
```

这比当前：
- `single-key + wildcard + km compare`
更自然，也更接近你最初设想。

---

## 第三阶段结构调整计划

### 1. 去掉 current single-key 规则
对应当前逻辑：
- [buildHashTables(...)](d:/work/2026/Hyperscan/code/hyperscan/src/fdr/pbe_compile.cpp)

删除：
```cpp
bucketKey = (keyMask == fullMask) ? keyValue : 0
```

### 2. 新增 mask-class 描述结构
建议新增 compile/runtime 结构，例如：

编译期：
- `PBEMaskClass`
  - `mask`
  - `keyBits`
  - `classId`
  - `primaryTable`
  - `secondaryRange`

运行期：
- `PBERuntimeMaskClass`
  - `mask`
  - `keyBits`
  - `primaryOffset`
  - `primaryCount`
  - `secondaryOffset`
  - `secondaryCount`

### 3. rule routing 改成“先 class，再 key”
编译期：
1. 先按 `keyMask` 分类
2. 对每个类按投影 key 建自己的 L1/L2

运行期：
1. 先拿 exact/full mask class
2. 再拿若干 partial mask class
3. 每个 class 用对应 mask 做 `BEXT`
4. 查该 class 的 L1/L2

### 4. `wildcardEncoded` 语义调整
如果走 mask-class 方案：
- 当前 `wildcardEncoded` 基本就可以消失
- 或者改成：
  - `residualEncoded`
  - 只给极少数无法纳入 mask-class 的规则兜底

### 5. `PBESecondaryHashEntry` 可能也要调整
因为当前：
- `keyValue`
- `keyMask`
- `headMask`
- `tailMask`
是围绕“single global key + wildcard verify”设计的

如果改成 mask-class：
1. `keyMask` 可以类级共享，减少冗余
2. `keyValue` 也可以按 class 的 projected key 存
3. L2 更像“同类同 key 下的 suffix/confirm 候选包”

### 6. 外层 FDR/Neo bucket 借壳也要一起瘦身
到了第三阶段，PBE 的内部 hash 已经和传统 FDR bucket 语义彻底脱钩。  
这时我建议开始评估：
1. 哪些 proto 外层信息还能留
2. 哪些已经纯属冗余
3. 是否把 `choosePbeEngine` 进一步降壳

---

## 第三阶段测试计划

新增重点测试：
1. `PBECompile.MaskClassRoutingWithoutWildcardZero`
2. `PBECompile.PartialMaskRulesDoNotCollapseToSingleBucket`
3. `PBERuntime.MaskClassProjectedKeyMatchesCompileTime`
4. `PBEvsNeo.PartialMaskClassConsistency`
5. `PBEvsNeo.NoWildcardBucketRequiredForSupportedPartialMasks`

---

## 第三阶段文档更新
会重点重写：
- [PBE_Engine_System_Analysis_Current.md](d:/work/2026/Hyperscan/code/hyperscan/documents/PBE_Engine_System_Analysis_Current.md)

新增：
1. mask-class 架构说明
2. 为什么它比 single-key/wildcard 更适合 PBE
3. 为什么它和 `BEXT` 更匹配
4. 新的 compile/runtime 数据流图

---

# 我建议的实施顺序

## 阶段 1
先做：
1. selector 顺序重排
2. 去掉 `bextToKeyBit`
3. 直接 `BEXT -> final key`
4. `SVEBITPERM` 批量提取接口

## 阶段 2
再做：
1. bitmap 批量 probe
2. compact
3. packed L1 load
4. 保序回放

## 阶段 3
最后做：
1. 去掉 single-key / wildcard
2. 引入 mask-class 路由
3. 重建 PBE 自己的直接按 bit 哈希结构

---

# 这三阶段里我认为最重要的设计决策

1. **第一阶段一定要先把 key 位顺序和 `BEXT` packed 顺序统一**
   - 不然第二、三阶段都会被 remap 拖着走

2. **第二阶段的 bitmap 优化不能只做“更快的 bit-test”**
   - 必须和 compact 绑定

3. **第三阶段不能简单从 wildcard 改成“规则展开”**
   - 你已经明确说了不走展开
   - 所以 mask-class 这种“按 cared bit 集合建子表”的思路更适合

---

如果你确认这份三阶段计划，我下一步就只开始实施**第一阶段**：
- 重构 `bext` 逻辑
- 去掉 `bextToKeyBit`
- 让 `BEXT` 直接生成最终 key

我会先从编译期 key 位顺序和 runtime 提取路径入手，不碰后两阶段。